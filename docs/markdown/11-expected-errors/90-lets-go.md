<!-- .slide: class="transition bg-blue" -->

# Time to stop treating "not found" like a crash

Notes:

Short beat before the lab. Recap the shape one more time — notFound() throws a recognized 404 interrupt, not-found.tsx renders in its place, it's a Server Component by default, per-segment files nest exactly like error.tsx did in module 10. Straight into wiring it up for real.
