<!-- .slide: class="transition bg-blue" -->

# Time to contain the damage

Notes:

Short beat before the lab. Recap the shape one more time — error.tsx is a Client Component, catches its segment and everything below, skips its own segment's layout — then the parallel-routes twist: each @slot is its own mini route, so it can get its own error.tsx too. Straight into wiring it up for real.
