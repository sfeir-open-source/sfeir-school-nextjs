<!-- .slide: class="transition bg-blue" -->

# Time to watch it happen

Notes:

Short beat before the lab. Recap the shape one more time — a Server Component render pauses on await, <Suspense> lets whatever's outside a boundary stream immediately while whatever's inside waits behind a fallback, cacheComponents is why every fetch in this app already has one. This lab has no new file to write — it's about breaking the pattern on purpose and watching what changes.
