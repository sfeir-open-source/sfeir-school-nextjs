<!-- .slide: class="transition bg-blue" -->

# Time to decide what gets baked

Notes:

Short beat before the lab. Recap the shape: generateStaticParams bakes a known, finite list of dynamic route values at build time; a subset is a legitimate choice, not a compromise; anything outside that list still falls back on module 11's notFound(). Then straight into wiring it up on employees and expenses.
