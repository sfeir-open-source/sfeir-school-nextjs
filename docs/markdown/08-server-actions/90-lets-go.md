<!-- .slide: class="transition bg-blue" -->

# Time to write, not just read

Notes:

Short beat before the lab. Recap the shape one more time — 'use server', FormData in, revalidateTag to clear the stale copy, redirect to leave the form — then straight into wiring it up for real.
