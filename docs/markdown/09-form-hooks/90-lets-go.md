<!-- .slide: class="transition bg-blue" -->

# Time to make the form talk back

Notes:

Short beat before the lab. Recap the shape one more time — useActionState wraps the action and hands back state to render, useFormStatus reads pending from a child of the form — then straight into wiring it up for real.
